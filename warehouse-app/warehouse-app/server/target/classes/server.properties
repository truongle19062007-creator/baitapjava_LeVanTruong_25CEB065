# Cấu hình Server Quản lý kho
# Copy file này vào cùng thư mục với file jar khi chạy server (java -jar server.jar)
# nếu muốn override giá trị mặc định trong code.

db.url=jdbc:mysql://localhost:3306/warehouse_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&characterEncoding=UTF-8
db.username=root
db.password=

server.port=9999
server.maxThreads=50
